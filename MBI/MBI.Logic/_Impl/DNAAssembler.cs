using System.Collections.Generic;

namespace MBI.Logic._Impl
{
	public class DNAAssembler : IDNAAssembler
	{
		public IEnumerable<DNAAssembly> Assemble(IEnumerable<string> contigs, IEnumerable<PairedEndTag> pairedEndTags)
		{
			throw new System.NotImplementedException();
		}
	}
}
